{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Build Project",
			"options": {
				"cwd": "${workspaceFolder}/dist",
			},
			"command": "rm -rf ${workspaceFolder}/dist/* && cmake .. && make",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}